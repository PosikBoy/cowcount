# 📄 ТЕХНИЧЕСКОЕ ЗАДАНИЕ

## Веб-система мониторинга коров с использованием нейросети YOLO

---

## 1. Общие сведения

### 1.1 Название проекта

**Веб-система мониторинга коров на основе компьютерного зрения**

### 1.2 Цель проекта

Разработка веб-приложения, позволяющего пользователю загружать фотографии коров, выполнять автоматическое распознавание объектов с помощью нейросети YOLO и сохранять историю распознаваний в базе данных.

---

## 2. Архитектура системы

### 2.1 Общая схема

```
[ Next.js (Frontend) ]
        │ axios
        ▼
[ NestJS (Backend API) ]
        │ axios
        ▼
[ Python YOLO Service ]
        │
        ▼
[ Результаты распознавания ]
        │
        ▼
[ MySQL + файловое хранилище ]
```

---

## 3. Используемые технологии

### 3.1 Frontend

- **Next.js (App Router)**
- TypeScript
- axios (HTTP-запросы)
- HTML5 / CSS3

### 3.2 Backend

- **NestJS**
- TypeScript
- axios (взаимодействие с ML-сервисом)
- Multer (загрузка файлов)
- TypeORM
- MySQL

### 3.3 ML-сервис

- Python 3.10+
- FastAPI
- YOLOv8 (Ultralytics)
- Pillow

### 3.4 Хранилище

- MySQL 8
- Локальная файловая система сервера

---

## 4. Функциональные требования

---

### 4.1 Загрузка изображений

**Описание:**
Пользователь должен иметь возможность загрузить изображение коровы через веб-интерфейс.

**Требования:**

- Поддерживаемые форматы: JPG, PNG
- Максимальный размер файла: 5 МБ
- Проверка MIME-типа файла
- Передача изображения на backend через HTTP POST

---

### 4.2 Распознавание объектов (YOLO)

**Описание:**
После загрузки изображения backend должен отправить файл в ML-сервис для распознавания объектов.

**Требования:**

- Использовать нейросеть YOLOv8
- Выделять коров на изображении
- Возвращать:
  - класс объекта
  - confidence
  - bounding box (xyxy)

---

### 4.3 Хранение изображений

**Описание:**
Загруженные изображения должны сохраняться на сервере.

**Требования:**

- Изображения сохраняются в директории `/uploads`
- В БД хранится **путь к файлу**, а не бинарные данные
- Имя файла должно быть уникальным (timestamp или UUID)

---

### 4.4 Хранение истории распознаваний

**Описание:**
Каждое распознавание должно сохраняться в базе данных.

**Структура таблицы `recognitions`:**

| Поле       | Тип      | Описание           |
| ---------- | -------- | ------------------ |
| id         | INT      | Идентификатор      |
| image_path | VARCHAR  | Путь к изображению |
| result     | JSON     | Результат YOLO     |
| cows_count | INT      | Количество коров   |
| created_at | DATETIME | Дата распознавания |

---

### 4.5 Просмотр истории распознаваний

**Описание:**
Пользователь должен иметь возможность просматривать список всех распознаваний.

**Требования:**

- Сортировка по дате (DESC)
- Отображение:
  - изображения
  - количества коров
  - даты и времени

---

## 5. API Backend (NestJS)

---

### 5.1 `POST /detect`

**Описание:**
Загрузка изображения и запуск распознавания.

**Входные данные:**

- `multipart/form-data`
- поле `file`

**Логика:**

1. Валидация файла
2. Сохранение изображения
3. Отправка файла в ML-сервис через axios
4. Обработка результата
5. Сохранение данных в MySQL
6. Возврат результата клиенту

**Ответ:**

```json
{
  "id": 1,
  "imagePath": "1700000000.jpg",
  "cowsCount": 3,
  "result": [...]
}
```

---

### 5.2 `GET /detect/history`

**Описание:**
Получение истории распознаваний.

**Ответ:**

```json
[
  {
    "id": 1,
    "imagePath": "1700000000.jpg",
    "cowsCount": 3,
    "createdAt": "2026-01-28T12:00:00Z"
  }
]
```

---

## 6. Использование axios

---

### 6.1 Frontend → Backend (Next.js)

```ts
import axios from "axios";

const formData = new FormData();
formData.append("file", file);

const res = await axios.post("http://localhost:3000/detect", formData);
```

---

### 6.2 Backend → ML-сервис (NestJS)

```ts
import axios from "axios";

const response = await axios.post("http://localhost:9000/detect", formData, {
  headers: formData.getHeaders(),
});
```

---

## 7. Нефункциональные требования

---

### 7.1 Производительность

- Обработка одного изображения ≤ 2 секунд (CPU)
- Одновременная работа минимум 3 пользователей

---

### 7.2 Безопасность

- Проверка типа загружаемых файлов
- Ограничение размера запроса
- Отсутствие выполнения пользовательского кода

---

### 7.3 Масштабируемость

- ML-сервис вынесен отдельно
- Возможность запуска нескольких ML-инстансов
- Возможность переноса ML-сервиса на отдельную VM

---

## 8. Развёртывание

---

### 8.1 Локальное развёртывание

- MySQL через Docker
- NestJS через `npm run start:dev`
- ML-сервис через `uvicorn`
- Next.js через `npm run dev`

---

### 8.2 Виртуальная машина

- ОС: Ubuntu 20.04+
- RAM: от 2 GB
- CPU inference
- Открытые порты: `3000`, `3001`, `9000`

---

## 9. Возможные расширения (необязательно)

- Дообучение YOLO под конкретное стадо
- Авторизация пользователей
- Статистика по датам
- Экспорт данных
- Docker Compose для всей системы

---

## 10. Ожидаемый результат

В результате реализации проекта должна быть создана веб-система, позволяющая выполнять автоматическое распознавание коров на изображениях, хранить историю распознаваний и предоставлять удобный интерфейс для просмотра результатов.

# 📄 ТЕХНИЧЕСКОЕ ЗАДАНИЕ (расширение)

## Визуальный интерфейс и стилизация

---

## 11. Пользовательский интерфейс (Frontend)

---

### 11.1 Общие требования к интерфейсу

- Интерфейс должен быть реализован на **Next.js**
- Для компонентов интерфейса должна использоваться библиотека **Material UI (MUI)**
- Стилизация должна выполняться с использованием **SCSS**
- Интерфейс должен быть адаптивным (desktop ≥ 1024px)
- Основной сценарий использования — загрузка изображения и просмотр результата распознавания

---

## 11.2 Используемые технологии (Frontend)

- Next.js (App Router)
- TypeScript
- **Material UI (MUI v5)**
- **SCSS (SCSS Modules)**
- axios

---

## 11.3 Структура интерфейса

### Основные экраны:

1. Экран распознавания
2. Экран истории распознаваний

---

## 11.4 Экран распознавания

### 11.4.1 Состав экрана

- Заголовок страницы
- Кнопка загрузки изображения
- Область предпросмотра изображения
- Блок визуализации результата распознавания
- Текстовое отображение количества распознанных коров

---

### 11.4.2 Загрузка изображения

**Требования:**

- Использовать компонент `Button` из Material UI
- Использовать `axios` для отправки запроса
- Поддержка drag-and-drop (необязательно)

---

### 11.4.3 Визуализация количества коров (КВАДРАТЫ)

**Описание:**
Количество распознанных коров должно отображаться в виде набора квадратов, где **один квадрат = одна корова**.

**Требования:**

- Квадраты располагаются в сетке
- Использовать Flexbox или CSS Grid
- Цвет квадратов — основной цвет темы
- При наведении отображается tooltip с номером коровы

---

### Пример логики отображения (frontend)

```ts
const cowsCount = result.cowsCount;

const squares = Array.from({ length: cowsCount });
```

---

### Пример JSX

```tsx
<Box className={styles.cowsGrid}>
  {squares.map((_, i) => (
    <Box key={i} className={styles.cowSquare}>
      {i + 1}
    </Box>
  ))}
</Box>
```

---

### SCSS (`Recognition.module.scss`)

```scss
.cowsGrid {
  display: grid;
  grid-template-columns: repeat(auto-fill, 40px);
  gap: 12px;
  margin-top: 16px;
}

.cowSquare {
  width: 40px;
  height: 40px;
  background-color: #1976d2; // MUI primary
  color: #fff;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 4px;
  font-weight: 600;
}
```

---

## 11.5 Использование Material UI

---

### 11.5.1 Обязательные компоненты MUI

| Компонент    | Назначение           |
| ------------ | -------------------- |
| `Button`     | Загрузка изображения |
| `Card`       | Блок распознавания   |
| `Typography` | Заголовки и текст    |
| `Box`        | Контейнеры           |
| `Tooltip`    | Подсказки            |

---

### Пример карточки распознавания

```tsx
<Card>
  <CardContent>
    <Typography variant="h6">Результат распознавания</Typography>

    <Typography>Обнаружено коров: {result.cowsCount}</Typography>

    <CowsGrid count={result.cowsCount} />
  </CardContent>
</Card>
```

---

## 11.6 SCSS и организация стилей

---

### Требования:

- Использовать **SCSS Modules**
- Каждый компонент имеет собственный файл стилей
- Запрещено использовать глобальные стили для компонентов

### Пример структуры

```
/components
  /Recognition
    Recognition.tsx
    Recognition.module.scss
```

---

## 11.7 Экран истории распознаваний (UI)

---

### Отображаемая информация:

- Миниатюра изображения
- Дата и время распознавания
- Количество коров (в виде квадратов)
- Кнопка просмотра подробностей

---

### Требования:

- Использовать `Card` из MUI
- Использовать `Grid` для раскладки
- Повторно использовать компонент квадратов

---

## 11.8 Темизация

---

### Требования:

- Использовать `ThemeProvider` из MUI
- Основной цвет темы — синий
- Поддержка расширения темы в будущем

---

### Пример

```tsx
const theme = createTheme({
  palette: {
    primary: {
      main: "#1976d2",
    },
  },
});
```

---

## 12. Требования к UX

- Пользователь должен сразу видеть результат распознавания
- Количество коров должно быть понятно **без чтения текста**
- Визуальные элементы не должны перегружать интерфейс
- Интерфейс должен корректно работать при количестве коров ≥ 10

---

## 13. Ожидаемый результат (дополнение)

В результате реализации пользователь получает визуально наглядный интерфейс, в котором количество распознанных коров отображается в виде квадратных индикаторов, что упрощает восприятие результатов распознавания.
