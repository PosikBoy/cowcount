# 🚀 Руководство по установке и запуску (Упрощенная версия)

## Пошаговая инструкция

### Шаг 1: Подготовка

```bash
cd cowcount
```

### Шаг 2: Настройка Backend (FastAPI + YOLO + SQLAlchemy)

```bash
cd ml-service

# Создание виртуального окружения
python3 -m venv venv

# Активация (macOS/Linux)
source venv/bin/activate

# Активация (Windows)
# venv\Scripts\activate

# Установка зависимостей
pip install -r requirements.txt

# Запуск сервиса
uvicorn main:app --host 0.0.0.0 --port 9000 --reload
```

Backend будет доступен на: **http://localhost:9000**

**Примечание:**

- При первом запуске YOLOv8 автоматически загрузит модель (~6 МБ)
- SQLite база данных создастся автоматически (`cowcount.db`)
- Не нужен Docker или отдельный MySQL!

### Шаг 3: Настройка Frontend (Next.js)

Откройте новый терминал:

```bash
cd frontend

# Установка зависимостей
npm install

# Запуск в режиме разработки
npm run dev
```

Frontend будет доступен на: **http://localhost:3001**

### Шаг 4: Проверка работы

1. Откройте браузер: **http://localhost:3001**
2. Загрузите изображение с коровой
3. Нажмите "Распознать коров"
4. Проверьте результат и историю

## 🔍 Проверка сервисов

### Backend

```bash
# Информация о API
curl http://localhost:9000/

# Health check
curl http://localhost:9000/health

# Статистика
curl http://localhost:9000/stats

# История
curl http://localhost:9000/detect/history
```

### Frontend

Откройте http://localhost:3001 в браузере

## 📊 Просмотр базы данных

```bash
# Установите sqlite3 (если еще не установлен)
# macOS: brew install sqlite
# Ubuntu: sudo apt-get install sqlite3

# Откройте базу данных
sqlite3 ml-service/cowcount.db

# Команды SQLite:
.tables                     # Показать все таблицы
.schema recognitions        # Показать структуру таблицы
SELECT * FROM recognitions; # Показать все записи
SELECT COUNT(*) FROM recognitions; # Подсчет записей
.exit                       # Выход
```

## 🐛 Решение проблем

### Проблема: Backend не запускается

1. Проверьте, что виртуальное окружение активировано:

```bash
which python  # Должен показать путь к venv
```

2. Переустановите зависимости:

```bash
pip install --upgrade pip
pip install -r requirements.txt
```

3. Проверьте версию Python:

```bash
python --version  # Должна быть 3.10+
```

### Проблема: Ошибка при загрузке YOLO модели

```bash
# Загрузите модель вручную
cd ml-service
source venv/bin/activate
python -c "from ultralytics import YOLO; YOLO('yolov8n.pt')"
```

### Проблема: Frontend не может подключиться к Backend

1. Убедитесь, что Backend запущен на порту 9000:

```bash
lsof -i :9000  # macOS/Linux
netstat -ano | findstr :9000  # Windows
```

2. Проверьте CORS в браузере (F12 → Console)

3. Попробуйте обратиться к API напрямую:

```bash
curl http://localhost:9000/health
```

### Проблема: Ошибка при загрузке изображения

1. Проверьте размер файла (максимум 5 МБ)
2. Убедитесь, что формат JPG или PNG
3. Проверьте, что директория `uploads/` существует:

```bash
ls -la uploads/
```

4. Проверьте права доступа:

```bash
chmod 755 uploads/
```

### Проблема: База данных не создается

1. Проверьте права на запись в директории:

```bash
cd ml-service
touch test.txt  # Если ошибка - нет прав на запись
rm test.txt
```

2. Создайте базу вручную:

```bash
cd ml-service
sqlite3 cowcount.db "CREATE TABLE IF NOT EXISTS recognitions (id INTEGER PRIMARY KEY);"
```

## 📝 Тестовые изображения

Для тестирования можно использовать:

- Поиск в Google: "cow image"
- Бесплатные стоки: [Unsplash](https://unsplash.com/s/photos/cow), [Pexels](https://www.pexels.com/search/cow/)
- Убедитесь, что на изображении видны коровы

## 🔄 Перезапуск сервисов

```bash
# Терминал 1: Backend
cd ml-service
source venv/bin/activate
uvicorn main:app --host 0.0.0.0 --port 9000 --reload

# Терминал 2: Frontend
cd frontend
npm run dev
```

## 📝 Порты

| Сервис   | Порт | URL                   |
| -------- | ---- | --------------------- |
| Frontend | 3001 | http://localhost:3001 |
| Backend  | 9000 | http://localhost:9000 |

## ✅ Контрольный список

- [ ] Python 3.10+ установлен
- [ ] Node.js 18+ установлен
- [ ] Виртуальное окружение создано
- [ ] Зависимости Python установлены
- [ ] Backend запущен (порт 9000)
- [ ] Зависимости npm установлены
- [ ] Frontend запущен (порт 3001)
- [ ] Директория uploads/ существует
- [ ] База данных cowcount.db создана

## 🎯 Следующие шаги

После успешного запуска:

1. Загрузите тестовое изображение с коровой
2. Проверьте распознавание
3. Просмотрите историю
4. Проверьте статистику: http://localhost:9000/stats
5. Изучите API документацию: http://localhost:9000/docs (автоматически генерируется FastAPI)

## 💡 Полезные команды

```bash
# Просмотр логов Backend
# Логи выводятся в терминале где запущен uvicorn

# Очистка базы данных
rm ml-service/cowcount.db
# База пересоздастся при следующем запуске

# Очистка загруженных изображений
rm -rf uploads/*
touch uploads/.gitkeep

# Обновление зависимостей
pip install --upgrade -r ml-service/requirements.txt
npm install --prefix frontend

# Просмотр API документации
open http://localhost:9000/docs  # Swagger UI
open http://localhost:9000/redoc # ReDoc
```

## 🌟 Преимущества упрощенной архитектуры

✅ **Всего 2 сервиса** вместо 3 (Frontend + Backend)
✅ **Не нужен Docker** для разработки
✅ **Не нужен MySQL** - SQLite работает из коробки
✅ **Быстрый старт** - меньше настроек
✅ **Проще отладка** - все в одном месте
✅ **Автоматическая документация** - FastAPI генерирует Swagger

## 🔧 Расширенная настройка

### Использование MySQL вместо SQLite

Если нужна MySQL для production:

1. Установите MySQL драйвер:

```bash
pip install pymysql
```

2. Установите переменную окружения:

```bash
export DATABASE_URL="mysql+pymysql://user:password@localhost/cowcount"
```

3. Перезапустите backend

### Использование PostgreSQL

```bash
pip install psycopg2-binary
export DATABASE_URL="postgresql://user:password@localhost/cowcount"
```

## 📚 Дополнительные ресурсы

- [FastAPI документация](https://fastapi.tiangolo.com/)
- [YOLOv8 документация](https://docs.ultralytics.com/)
- [Next.js документация](https://nextjs.org/docs)
- [Material UI документация](https://mui.com/)
- [SQLAlchemy документация](https://docs.sqlalchemy.org/)
